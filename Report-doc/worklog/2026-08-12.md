# Worklog — 2026-08-12

**Focus:** the confidence-analysis notebook's chart request led to a real
scoring-notebook bug — confusion matrix rows silently omitted a whole category
and never summed to 100%. Chasing that down led to two separate findings about
*why* items go unlabeled: one is an extraction limit shared by every model,
the other is a scoring-rule side effect that is not shared at all.

**Status at end of day:** both comparison notebooks (75% and 100% overlap)
rebuilt and executing clean at 38 cells each. Confidence notebook fixed and
extended. New threshold-comparison notebook added. No pipeline re-runs —
everything today is re-scoring and re-drawing of existing stage 3/4 output.

---

## 1. Confidence notebook — a break from the user's own manual trim, then a redesign

The user had hand-trimmed `notebooks/5-confidence-analysis.ipynb` since the
last build, deleting the four-boxes cell. The surviving `examples` cell still
referenced `df['high confidence']`, a column only that deleted cell created —
a `KeyError` waiting for the next full re-run. Fixed by assigning the column
inline in `examples` instead of restoring the deleted cell.

**The requested chart** ("mean confidence, correct vs incorrect, per model")
went through two design passes:

- v1: grouped bar chart, green/red, ±1 SD error bars. Two problems found before
  shipping: the gap between the two bars is 0.4–4.8 points against a 0–100 axis
  that a bar chart must start at, so it was nearly invisible; and green/red
  validates at ΔE 8.0 under deuteranopia (`scripts/validate_palette.js`) — the
  bare legal floor, not a safe choice, and green also failed contrast against
  the page background.
- v2: dot-and-range plot, blue/orange (ΔE 24, passes clean), spread shown as
  the 10th–90th percentile instead of ±1 SD. The SD version had bars running
  past 100%, which is impossible for a bounded percentage — most items sit at
  the ceiling, so the distribution isn't symmetric. Percentiles stay inside the
  real range and made the finding sharper: every band's right edge sits at
  exactly 100%, and for llama3.3:70b the correct-answer band and the
  incorrect-answer band are **identical** (70–100% either way).

Also added the mean(SD) table in the exact column order requested
(`Confidence when incorrect (%), mean (SD)` before the correct-answer column),
rendered as a single styled table with the chart's title as its caption.

### A separate filename bug, unrelated to the above

`scripts/build/build_confidence_notebook.py` still wrote to
`notebooks/8-confidence-analysis.ipynb` — the name from before the user
renamed the notebook to `5-` in an earlier session. Every rebuild was silently
**recreating the file the user had deliberately renamed away**, while `5-`
never changed. All of today's confidence-notebook edits were being verified
against the wrong file for a while before this was caught. Fixed the hardcoded
path and deleted the stale `8-` file.

---

## 2. New notebook — `6-threshold-comparison.ipynb`

Answers directly: what changes when a match must be 100% of the predicted
words instead of 75%?

| model | Path A 75% | Path A 100% | Path B 75% | Path B 100% |
|---|---|---|---|---|
| llama3.1:8b | 0.298 | 0.287 | 0.323 | 0.313 |
| gemma4:e4b | 0.689 | 0.656 | 0.692 | 0.665 |
| llama3.3:70b | 0.737 | 0.717 | 0.737 | 0.721 |

Every score falls (0.010–0.033, above the ±0.002 noise floor) but the ranking
never changes. Traced every item that flips between the two thresholds to one
of three causes: PDF text-layer damage (a word split across a line break, two
words run together), one confirmed typo in the annotation itself (it drops the
word "protect" that is genuinely in the PDF, so the strict threshold penalises
a model for being right), and genuine fused headings — the only one of the
three that is a real labeling-relevant error.

---

## 3. The confusion-matrix bug: rows didn't sum to 100%

### The defect

`build_pct()` in `build_comparison_notebook.py` divided by the row total
(predictions + never-produced) to make the diagonal read as recall, but only
ever **drew** the five label columns. The `__missed__` share was in the
denominator and invisible in the picture — so `section.title` for llama3.1:8b
showed 88%, correctly computed, with the missing 12% simply never drawn.

Confirmed the size of what was hidden — up to 43% of a row (`question.text`,
llama3.1:8b, Path B: 24 of 56 real questions never produced anything at all).

### The fix

Added a sixth column, `(not labeled)` (renamed from an initial `(not
produced)` at the user's request), set off with a heavy divider rule since it
is not a label the model chose. A new `verify-100` cell asserts every row now
sums to exactly 100%, computed from the raw confusion dict rather than trusted
by eye.

### Section 4 — which items, not just how many

Added a full accounting: a per-class percentage summary, the actual list of
missing annotation items, and a per-sample breakdown. That last one produced
the clearest number of the day — pooled across all three models at 100%
overlap, **samples 2 and 6 account for 28 of 36 Path A missing items (78%)**;
the other eight documents contribute almost nothing.

### The raw-count matrix and a second, related shading bug

Added a second matrix below the percentage one, showing the literal counts —
requested so slides can carry both. First version shaded each row against its
own row maximum, which meant a 7 in a small row (`section.description`, 8
items total) rendered the same darkness as a 35 in a big row (`section.title`,
43 items) — same color, very different numbers. Fixed to one shared scale
across every panel with a real colorbar, so a given shade means the same count
everywhere in the figure.

---

## 4. Two different reasons items go unlabeled — verified, not assumed

Prompted by the question "why do all three models miss the *same* items in
Path A?" — checked whether it was really the same items or just a matching
count, by comparing the actual sets.

**Path A, sample 6: genuinely identical**, set-equality confirmed, not just
equal counts. All three models miss the exact same 8 items at 75% overlap.
Cause: stage 1 extraction is keyed by extractor only, not by model, so all
three models read one shared file. An underlined heading in sample 6 arrives
on the same physical line as the start of its answer — pdfplumber output,
before any model runs — so no label choice can separate them; whichever label
wins, the merged block matches the answer's words, not the heading's.
Confirmed this identity breaks down at the 100% threshold (11/13/12 items,
only 10 of 14 shared) — stricter matching starts being sensitive to how each
model's own merges land, so identical counts is a real but narrow signature of
fused blocks, not a general property.

Went one level deeper on the same document: on the parts extraction did
**not** damage, the three models are clearly not the same —
llama3.3:70b gets 6/11 items right on sample 6 overall, gemma4:e4b 3/11,
llama3.1:8b 1/11. "All three models are the same on sample 6" is only true for
the six items extraction pre-decided.

**Path B: differences are model-driven, and the mechanism is a scoring
artifact.** The annotation rule fills a blank `question.text` from the section
title, but only when the model's own field was empty. Traced two items in
sample 2 (`"2. Data used in publications"`, `"4. Confidentiality, security and
rights"`) where llama3.1:8b and llama3.3:70b both wrote a real answer
paragraph into `question.text` — correct behavior — so the rule left it alone,
and it doesn't match the annotation's title-repeat expectation, scoring as
missing. gemma4:e4b left the field blank in both cases, the rule auto-filled
it with the title text, and it matched by coincidence. **The model that
answered less is rewarded; the two that answered more are penalised** — a
side effect of the rule, not a capability difference. Added a live worked
example of this (section 5, both notebooks) that inspects each model's raw
stage-3 JSON and prints the fill state directly, rather than asserting it.

**Then checked whether this generalizes beyond the two documents used as the
example — it does not stay contained.** Path B has 81 missing items pooled
across all three models at 100% overlap, more than double Path A's 36, and
spread across **8 of the 10 documents** rather than 4. Samples 2 and 6 are
still the largest single contributors but are only 57% of the Path B total,
down from being nearly all of Path A's. Not yet added to the notebook — the
section 5 conclusion currently reads as if the effect is contained to the two
example documents, which understates it.

---

## 5. Next steps

1. **Fix the section 5 conclusion in both comparison notebooks** — it should
   state plainly that Path B's rule-driven misses span 8 of 10 documents, not
   just the two used as the worked example.
2. **Consider scoring Path A's stage-3 output against the *new* annotation** —
   raised by the advisor as "compare Path A with the new DMP annotations to
   show improvement." No existing function does this; `load_method_new` always
   reads stage 4. Would isolate how much of the Path A→B gap is the rules
   versus just the annotation revision. Not built yet — flagged, not started.
3. Prompt reversion still open from 6 August (unchanged from prior worklogs).
4. Stage-1 underline/hanging-indent fixes from 7–8 August still unimplemented.
5. `2026-08-11` remains unlogged.

---

## Housekeeping

- No pipeline runs today — everything is re-scoring and re-drawing of existing
  stage 3/4 output plus new confusion-matrix code.
- `scripts/build/build_confidence_notebook.py` now writes to `5-` with a
  comment warning that a future renumber must update it manually, so this
  class of bug does not repeat a third time.
- Colour choices in new charts (`build_confidence_notebook.py`,
  `build_comparison_notebook.py`) were checked with the project's palette
  validator rather than picked by eye.
